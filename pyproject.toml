[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yara-research"
version = "0.1.0"
description = "Multi-agent research assistant with hybrid retrieval"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Jyothi Sai Sravana Ravella" }]
keywords = ["rag", "multi-agent", "retrieval", "llm", "research"]
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "numpy>=1.24.0",
    "scikit-learn>=1.2.0",
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "httpx>=0.26.0",
]

[project.optional-dependencies]
pdf = ["pypdf>=4.0.0"]
neural = ["sentence-transformers>=2.3.0"]
faiss = ["faiss-cpu>=1.7.4"]
dev = ["pytest>=7.4.0", "pytest-cov>=4.1.0", "ruff>=0.3.0"]

[project.urls]
Repository = "https://github.com/ravellasravana/YARA"

[tool.setuptools.packages.find]
include = ["yara*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
filterwarnings = ["ignore::DeprecationWarning"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
# Errors, pyflakes, import order, bugbear. Not SIM/UP - they mostly argue
# about style and the arguments are not worth having.
select = ["E", "F", "I", "B"]
ignore = ["E501"]

[tool.coverage.run]
source = ["yara"]
omit = ["yara/cli.py", "*/__init__.py"]
